<%content_for :content_page do%>
    <section class="invoice">
      <!-- title row -->
      <div class="row">
        <div class="col-xs-12">
          <h2 class="page-header">
            <i class="fa  fa-users"></i>  Registered Users
            <small class="pull-right"><b><%=Time.now%></b></small>
          </h2>
        </div>
      </div>
      <div class="row">
        <div class="col-xs-12 table-responsive">
          <table class="table table-striped">
            <thead>
            <tr>
              <th>Given Name</th>
              <th>Family Name</th>
              <th>email</th>
              <th>City</th>
              <th>Province</th>
              <th>Admin</th>
              <th></th>
            </tr>
            </thead>
            <tbody>
    			<%@users.each do |user|%>
					<tr>
	    				<td><%=user.given_name%></td>
	    				<td><%=user.family_name%></td>
	    				<td><%=user.email%></td>
	    				<td><%=user.city%></td>
	    				<td><%=user.province%></td>
	    				<td>
	    					<i class="fa <%=UtilityHelper.check_box(user.admin)%>"></i>
	    				</td>
	    				<td>
		                  <%=link_to resume_path(id: user.id),class: "btn btn-primary btn-xs" do%>
	                      		<i class="fa fa-search"></i>
	                      <%end%>	     					
	    				</td>						
					</tr>    			

    			<%end%>
            </tbody>
          </table>
        </div>
        <!-- /.col -->
      </div>
    </section>
<%end%>
