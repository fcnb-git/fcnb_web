<%= form_with(model: skill_inventory, local: true) do |form| %>
  <% if skill_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill_inventory.errors.count, "error") %> prohibited this skill_inventory from being saved:</h2>

      <ul>
      <% skill_inventory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :skill_inventory_category %>
  </div>

  <div class="field">
    <%= form.label :specific_skill %>
    <%= form.text_field :specific_skill, id: :skill_inventory_specific_skill %>
  </div>

  <div class="field">
    <%= form.label :remarks %>
    <%= form.text_field :remarks, id: :skill_inventory_remarks %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
